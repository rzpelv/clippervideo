node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.vite
*.log

# ffmpeg-core is fetched at build time by scripts/setup-ffmpeg.mjs.
# Don't commit the binary blobs; they regenerate per build.
public/ffmpeg/
